'use client'

import React, { useState } from 'react'

const ImageLightbox = ({ image }: { image: string }) => {
  const [modalOpen, setModalOpen] = useState(false)

  return (
    <div>
    </div>
  )
}

export default ImageLightbox
